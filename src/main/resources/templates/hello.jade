-@ var youAreUsingJade : Boolean = false
-@ var pageTitle : String = "??"
!!! 5
html(lang="en")
  head
    title= pageTitle

    link{ :rel=> "stylesheet", :href=>"/assets/bootstrap/css/bootstrap.css" }

    :javascript
      if (foo) {
         bar()
      }
  body
    h1 Jade - node template engine
    #container
      - if (youAreUsingJade)
        p You are amazing
      - else
        p Get on it!
    :coffeescript
      alert "Hello, Coffee!"